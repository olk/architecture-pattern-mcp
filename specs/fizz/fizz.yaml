# specs/fizz/fizz.yaml — suite-wide FizzBee defaults (fizzbee plan §3.3).
# Per-spec frontmatter may tighten but not loosen these bounds; budget diffs
# are reviewed like code (principle 4/5, fizzbee plan §3.1).
options:
  max_actions: 2000            # hard stop; a spec hitting this is a budget bug
  max_concurrent_actions: 2    # the F6 gate level; nightly simulation may raise to 3
